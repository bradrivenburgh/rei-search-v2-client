import React, { useState } from 'react';
import { Map as MapLeaflet, TileLayer, Marker, Popup } from 'react-leaflet';
import './Map.css';

function Map() {
  


  return(
    <div className="map">
      <h2>MAP</h2>
    </div>
  );
}

export default Map;